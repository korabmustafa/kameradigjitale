export function InfoPage() {
  return (
    <main className="mx-auto max-w-5xl px-6 py-8">
      <section className="rounded-2xl bg-white p-8 shadow-playful">
        <h1 className="mb-4 text-3xl font-black">About Kamera Digjitale</h1>
        <p className="text-slate-700">
          This frontend is intentionally simple and API-ready. For now, products, cart, and admin actions run on local state.
          Once your backend is ready, swap in real API calls and keep the same UI flows.
        </p>
      </section>
    </main>
  )
}
